# Example configuration for Go-Grafana Webpage Monitor

# Comma-separated list of URLs to monitor
URLS=https://example.com,https://another.com

# How often to check the URLs (e.g., 60s, 5m)
CHECK_INTERVAL=60s

# SMTP server details for sending email alerts
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=youruser@example.com
SMTP_PASS=yourpassword

# Email addresses
SMTP_TO=alertrecipient@example.com
SMTP_FROM=monitor@example.com

# Number of consecutive failures before sending a DOWN alert (default: 2)
ALERT_THRESHOLD=2